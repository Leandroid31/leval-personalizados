name: leval_personalizados
description: App de pedidos de bolo com Firebase e exportação JPG

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.5.0
  firebase_auth: ^4.2.0
  cloud_firestore: ^4.4.5
  firebase_storage: ^11.1.0
  image_picker: ^1.0.0
  path_provider: ^2.1.1
  permission_handler: ^11.0.0
  screenshot: ^1.5.0
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
